# Smoke test: self-call _docker-buildx.yml against fixtures/docker.
name: smoke

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  inline-sanity:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - name: Confirm smoke triggers reach a job
        run: echo "smoke wiring OK"
