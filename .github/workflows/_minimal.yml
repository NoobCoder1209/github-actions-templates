name: minimal-callee
on:
  workflow_call:
    inputs:
      image:
        type: string
        required: true

jobs:
  run:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@v6
